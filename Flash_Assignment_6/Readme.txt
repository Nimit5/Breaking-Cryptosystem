Dependencies:

- sage : We assume that sage is already set up in your system.
- python 

How to run:

Open the current directory in the terminal and type "make run", this command will execute flash.py python script which will decrypt the password and display it on the terminal.

Estimated script runtime : 2 minutes(atmost)
