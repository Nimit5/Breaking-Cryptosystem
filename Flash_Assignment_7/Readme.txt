How to run:

Open the current directory in the terminal and type "make run" this command will run the script inside it. This python script will decrypt the password and display it the terminal.

We are also submitting jupyter notebook along with python file, so that you can verify our outputs.
